<mvc:View controllerName="com.kaar.ehsm.controller.Incidents"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:l="sap.ui.layout"
    xmlns:core="sap.ui.core"
    displayBlock="true">
    <Page id="incidentsPage" title="Incident Management" showNavButton="true" navButtonPress=".onNavBack">
        <content>
             <Table id="incidentsTable" items="{/ZEHSM_INCIDENT_GPSet}" width="auto" class="sapUiResponsiveMargin">
                <headerToolbar>
                    <Toolbar>
                        <Title text="Incidents History"/>
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column>
                        <Text text="Incident ID" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="Description" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="Status" />
                    </Column>
                    <Column hAlign="End">
                        <Text text="Priority" />
                    </Column>
                    <Column hAlign="End" minScreenWidth="Desktop" demandPopin="true">
                        <Text text="Date" />
                    </Column>
                </columns>
                <items>
                    <ColumnListItem type="Navigation" press=".onIncidentItemPress">
                        <cells>
                            <ObjectIdentifier title="{IncidentId}"/>
                            <Text text="{IncidentDescription}" />
                            <ObjectStatus text="{IncidentStatus}" state="{path: 'IncidentStatus', formatter: '.formatter.statusState'}" />
                            <ObjectStatus text="{IncidentPriority}" state="{path: 'IncidentPriority', formatter: '.formatter.priorityState'}" />
                            <Text text="{path: 'IncidentDate', type: 'sap.ui.model.type.Date', formatOptions: { style: 'medium' }}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View>
